import { Footer, HeroText, Navbar, ProductList } from "@/components";

export default function Home() {
  return (
    <div>
      <Navbar />
      <HeroText />
      <ProductList />
      <Footer />
    </div>
  );
}
